version: "3.8"

services:
  cryptotrade-bot:
    build: .
    container_name: cryptotrade-bot
    restart: always
    ports:
      - "8788:8788"
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
    env_file:
      - .env
